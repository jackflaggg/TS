let list: number[] = [1, 2, 3];

let listOne: Array<number> = [2, 34];

let y: [string, number] = ['yelf', 1]

let z: Array<any> = [1, false, 'not'];

enum Directions{
    Up= 2,
    Down= 4,
    Left= 6,
    Right,
}

Directions.Up;
Directions.Down;
Directions.Left;
Directions.Right;

type Name = string;
let id: Name;